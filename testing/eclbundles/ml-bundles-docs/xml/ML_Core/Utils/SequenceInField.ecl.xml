<?xml version='1.0' encoding='UTF-8'?>
<Meta>
 <Source name="ML_Core.Utils.SequenceInField" sourcePath="/media/sarthak/Data/ecldoc/testing/eclbundles/ml-bundles/ML_Core/Utils/SequenceInField.ecl" target="SequenceInField.ecl.xml">
  <Definition exported="1" fullname="ml_core.utils.sequenceinfield" inherittype="local" line="16" name="SequenceInField" type="macro">
   <Type type="boolean"/>
   <Documentation>
    <content>Given a file which is sorted by the work item identifier and INFIELD (and possibly other values), add sequence numbers within the range of each infield. Slighly elaborate code is to avoid having to partition the data to one value of infield per node and to work with very large numbers of records where a global count project would be inappropriate. This is useful for assigning rank positions with the groupings.</content><return>a file of the same type with sequence numbers applied</return><param>infile the input file, any type</param><param>infield field name of grouping field</param><param>seq name of the field to receive the sequence number</param><param>wi_name work item field name, default is wi</param><firstline>Given a file which is sorted by the work item identifier and INFIELD (and possibly other values), add sequence numbers within the range of each infield</firstline></Documentation>
   <Signature name="SequenceInField" ret="" param="(infile,infield,seq,wi_name='wi')" hlen="15">SequenceInField(infile,infield,seq,wi_name='wi')</Signature><Params>
    <Param name="infile">
     <Type type="integer8"/>
    </Param>
    <Param name="infield">
     <Type type="integer8"/>
    </Param>
    <Param name="seq">
     <Type type="integer8"/>
    </Param>
    <Param name="wi_name">
     <Type type="integer8"/>
    </Param>
   </Params>
  </Definition>
 <Documentation>
    <content>Given a file which is sorted by the work item identifier and INFIELD (and possibly other values), add sequence numbers within the range of each infield. Slighly elaborate code is to avoid having to partition the data to one value of infield per node and to work with very large numbers of records where a global count project would be inappropriate. This is useful for assigning rank positions with the groupings.</content><return>a file of the same type with sequence numbers applied</return><param>infile the input file, any type</param><param>infield field name of grouping field</param><param>seq name of the field to receive the sequence number</param><param>wi_name work item field name, default is wi</param><firstline>Given a file which is sorted by the work item identifier and INFIELD (and possibly other values), add sequence numbers within the range of each infield</firstline></Documentation>
   </Source>
</Meta>